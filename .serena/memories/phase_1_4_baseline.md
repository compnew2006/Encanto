Implemented and verified the Phase 1-4 baseline. Repo now has a Go backend (chi, pgx, sqlc, Redis-backed refresh sessions), a SvelteKit 5 frontend, root Playwright E2E, Docker Compose infra, and a shared permission catalog at backend/shared/permission_catalog.json. Local defaults were moved to backend 58080, Postgres 55432, Redis 56379 to avoid host conflicts. Key shipped flows: login/refresh/logout, org switching, /api/me bootstrap, settings/profile shell, roles/users management, chat list/detail/messages/notes read surfaces, read-only composer gating, scoped visibility, and deterministic seed users (admin/internal/readonly/scoped). Full verification passed: go test ./..., npm run check:frontend, npm run lint:frontend, npm run test:frontend, npx playwright test --project=chromium, npx playwright test (15 passed).